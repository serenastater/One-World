<div class="container">
<h1>Available Housing</h1>
<!-- <table>
  <thead>
    <tr>
      <th>Headline</th>
      <th>Street address</th>
      <th>City</th>
      <th>Country</th>
      <th>Description</th>
      <th>Accommodates</th>
      <th>Availability</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->
  <div class="row">
    <% @listings.each do |listing| %>
     <div class="col s12 m7 l6">

       <div class="card">

         <div class="card-image">

             <%= image_tag listing.listing_images.first.picture %>
           </div>
            <span class="card-title">
              <%= listing.headline %>
            </span>
            <div class="card-content">
              <%= listing.description %>
            </div>
             <%= listing.accommodates %>




           <p>I am a very simple card. I am good at containing small bits of information.
           I am convenient because I require little markup to use effectively.</p>
         </div>
         <div class="card-action">
           <a href="#">This is a link</a>
         </div>
       </div>
       <% end %>
     </div>
   </div>

  <tbody>

      <tr>

        <td><%#= listing.address.street_address %></td>
        <td><%#= listing.address.city %></td>
        <td><%#= listing.address.country %></td>


        <td><%#= listing.availability %></td>
        <td><%#= image_tag listing.listing_images.first.picture %></td>

        <td><%#= link_to 'Show', listing %></td>
        <td><%#= link_to 'Edit', edit_listing_path(listing) %></td>
        <td><%#= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <%# end%>

  </tbody>
</table>

<br>
</div>
<%= link_to 'New Listing', new_listing_path %> |
<%= link_to 'Home', current_user, class: 'btn-submit' %>
