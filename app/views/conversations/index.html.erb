
<div class="container">
<h1>Messages</h1>

<div class="col s9">
<div class="input-field">
  <ul class="tabs">
    <li class="tab col s3"><%= mailbox_section 'inbox', @box %></li>
    <li class="tab col s3"><%= mailbox_section 'sent', @box %></li>
    <li class="tab col s3"><%= mailbox_section 'trash', @box %></li>
  </ul>
</div>
</div>
<div>
  <% if @box == 'trash' %>
    <p><%= link_to 'Empty trash', empty_trash_conversations_path, class: 'btn-submit', method: :delete, data: {confirm: 'Are you sure?'} %></p>
    <% end %>
  </div>
  <div>
    <ul class="list-group">
      <%= render partial: 'conversations/conversation', collection: @conversations %>
    </ul>
</div>
<%# @conversations.each do |conversation| %>
<div>
  <%#= link_to conversation.subject, conversation_path(conversation) %>
</div>
<%# end%>

<%= link_to 'New Message', new_message_path %> |

<%= link_to 'Home', current_user, class: 'btn-submit' %>
</div>
