<% conversation.participants.each do |participant| %>
  <% unless participant == current_user %>
    <%= participant.first_name %>
    <img class="convo-header-img" <%= image_tag participant.avatar.url(:small) %>
  <% end %>
  <% end %>
